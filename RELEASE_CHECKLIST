

* Update version number in package.json and src/about_popup.tsx
* Run `npm update; npm install`
* Install the WASM blob (`./build_wasm.sh -i`)
* git commit add
* git tag -s v{your version}; git push; git push --tags
* npm publish --access public
